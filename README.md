# networksecurity- install
sudo apt update
sudo apt install python3-pip libpcap-dev -y
pip3 install scikit-learn numpy scapy

sudo pip3 install scikit-learn numpy scapy

run 
# Dynamic defense injection
os.system(f"sudo ufw insert 1 deny from {ip} to any")
python3 network.py

Bash
Save Alerts to a Log File (Best Practice)
sudo python3 network_ai_agent.py >> /var/log/ai_agent.log 2>&1

# View the live alerts as they happen in the file
tail -f /var/log/ai_agent.log

# Search the file specifically for the AI alert blocks
grep -A 5 "NETWORK AI ALERT" /var/log/ai_agent.log

screen -S security

sudo python3 network_ai_agent.py

screen -r security
